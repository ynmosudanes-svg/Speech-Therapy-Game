# Speech-Therapy-Game

Speech therapy web application with:

- `frontend/`: React + Vite interface
- `backend/`: Node.js + Express API

## Local development

### Frontend

```bash
cd frontend
npm install
npm run dev
```

### Backend

```bash
cd backend
npm install
npm start
```

Use `backend/.env.example` as the starting point for backend environment variables.
